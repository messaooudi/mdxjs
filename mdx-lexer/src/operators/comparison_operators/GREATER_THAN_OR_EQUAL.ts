import { ITokenConfig } from "chevrotain"

const GREATER_THAN_OR_EQUAL: ITokenConfig = {
    label: "GREATER_THAN_OR_EQUAL comparison operator",
    name: "greater than or equal",
    pattern: />=/
};

export default GREATER_THAN_OR_EQUAL;